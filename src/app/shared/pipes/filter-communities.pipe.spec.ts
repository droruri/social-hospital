import { FilterCommunitiesPipe } from './filter-communities.pipe';

describe('FilterCommunitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommunitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
